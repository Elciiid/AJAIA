# Walkthrough Video

**URL:** _paste your unlisted Loom or YouTube link here_

Suggested 3–5 minute outline (covers the rubric):

1. **Main user flow (~1.5 min)** — sign in as Alice → create a doc → format text
   (bold/italic/underline, H1/H2, lists) → rename → show autosave + refresh
   persistence.
2. **File import (~30s)** — import a `.md` file and show it became an editable
   document with formatting preserved.
3. **Sharing & access control (~1.5 min)** — open Share, add Carol as Viewer and
   Ben as Editor → sign in as Carol (read-only, View-only badge) → sign in as Ben
   (can edit). Point out the Owned vs. Shared split.
4. **What I deprioritized & why (~30s)** — real-time collab, passwords; depth
   over breadth.
5. **How AI supported the workflow (~30s)** — scaffolding speed, the pure tested
   access layer, browser-driven verification, and the version/library calls I
   overrode (Prisma 6, Radix preset).
